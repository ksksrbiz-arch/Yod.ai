# openyoda (Python SDK)

```python
from openyoda import Yoda
yoda = Yoda(api_key="...")
response = yoda.ask("should I take the offer?")
```

> Status: scaffold. Build with `uv build` or `pip install -e .`.

PyPI package: `openyoda`.
