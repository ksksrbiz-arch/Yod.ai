{
  "name": "@openyoda/mcp-server",
  "version": "0.0.1",
  "description": "Model Context Protocol server. Lets external agents (Claude, Cursor, etc.) call into Yoda's wisdom tools.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "MIT",
  "bin": { "openyoda-mcp": "src/server.ts" },
  "scripts": {
    "build": "echo 'mcp-server: build not implemented'",
    "test": "echo 'mcp-server: tests not implemented'",
    "lint": "echo 'mcp-server: lint not implemented'"
  },
  "dependencies": {
    "@openyoda/core": "*",
    "@openyoda/prompts": "*"
  }
}
